server {
    listen 80;

    location / {
        root   /usr/share/nginx/html;
        index  index.html index.htm;

        # ⬇️ THIS IS THE MAGIC LINE ⬇️
        # It says: "Try to find the file. If you can't, serve index.html"
        try_files $uri $uri/ /index.html;
    }
}